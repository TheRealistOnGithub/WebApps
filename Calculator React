import

class Calc extends React.Component {
    constructor(props) {
        super(props);

    }


    render() {

        return (
            <input type="text" value={this.state.num1} onChange={(event) => {
                c
                this.setState((oldState, props) => ({
                    num1: event.target.value,
                    num2: oldState.num2,
                    answer: oldState.answer
                }))
            }
            }/>
            < /div>

        );


    }

    ReactDOM.render
}